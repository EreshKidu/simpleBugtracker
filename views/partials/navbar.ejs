<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">SBT</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link <% if (pageName && locals.pageName === 'projects') { %> active <% } %> " href="/projects">Projects</a>
          <a class="nav-link <% if (pageName && pageName === 'about') { %> active <% } %> " href="/about">About</a>
        </div>
        <div class="navbar-nav ms-auto">
         
          
          <% if (!currentUser) {%> 
            <a href="/login" class="nav-link <% if (pageName && pageName === 'login') { %> active <% } %>">Login</a>
            <a href="/register" class="nav-link <% if (pageName && pageName === 'register') { %> active <% } %>">Register</a>
            <% } else { %>
            <a href="" class="nav-link"> <%= currentUser.email %></a>
            <a href="/logout" class="nav-link">Logout</a>
            <% } %> 

        </div>
      </div>
    </div>
  </nav>

